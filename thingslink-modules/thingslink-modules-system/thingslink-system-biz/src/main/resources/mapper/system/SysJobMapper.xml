<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thingslink.system.mapper.SysJobMapper">

	<resultMap type="SysJob" id="JobPO">
		<id property="jobId" column="job_id"/>
		<result property="jobName" column="job_name"/>
		<result property="jobGroup" column="job_group"/>
		<result property="invokeTarget" column="invoke_target"/>
		<result property="cronExpression" column="cron_expression"/>
		<result property="misfirePolicy" column="misfire_policy"/>
		<result property="concurrent" column="concurrent"/>
		<result property="status" column="status"/>
		<result property="createAt" column="create_at"/>
		<result property="createId" column="create_id"/>
		<result property="updateAt" column="update_at"/>
		<result property="updateId" column="update_id"/>
	</resultMap>

	<sql id="selectJobPO">
		SELECT job_id, job_name, job_group, invoke_target, cron_expression, misfire_policy, concurrent, status,
		create_at, create_id, update_at, update_id
		FROM sys_job
	</sql>

	<select id="list" parameterType="SysJob" resultMap="JobPO">
		<include refid="selectJobPO"/>
		<where>
			<if test="jobName != null and jobName != ''">
				AND job_name LIKE CONCAT('%', #{jobName}, '%')
			</if>
			<if test="jobGroup != null and jobGroup != ''">
				AND job_group = #{jobGroup}
			</if>
			<if test="status != null and status != ''">
				AND status = #{status}
			</if>
			<if test="invokeTarget != null and invokeTarget != ''">
				AND invoke_target LIKE CONCAT('%', #{invokeTarget}, '%')
			</if>
		</where>
		ORDER BY job_id DESC
	</select>

</mapper>
