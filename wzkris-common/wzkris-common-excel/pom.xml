<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.wzkris</groupId>
        <artifactId>wzkris-common</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>wzkris-common-excel</artifactId>

    <description>
        该模块复制于 https://gitee.com/dromara/RuoYi-Cloud-Plus.git
    </description>

    <properties>
        <excel.version>4.0.2</excel.version>
    </properties>

    <dependencies>

        <dependency>
            <groupId>com.wzkris</groupId>
            <artifactId>wzkris-common-core</artifactId>
        </dependency>

        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>easyexcel</artifactId>
            <version>${excel.version}</version>
        </dependency>

        <!-- 参数校验（可选依赖，用于Excel导入时的数据校验） -->
        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
            <optional>true</optional>
        </dependency>

    </dependencies>

</project>
